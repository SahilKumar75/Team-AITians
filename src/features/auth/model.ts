/**
 * Auth feature — model (types).
 * AuthUser and AuthContextType live in AuthContext; re-export for feature consumers.
 */
export type { AuthUser, AuthContextType } from "@/contexts/AuthContext";
